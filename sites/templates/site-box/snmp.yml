# Auth profiles only — merge with Alloy's embedded snmp_exporter modules (if_mib, etc.).
# Do NOT redefine modules here unless they include full metrics blocks from snmp_exporter generator.
# config_merge_strategy = "merge" is set in generate-config.sh.
#
# Change community to match your Fortinet / switch SNMPv2c string.

auths:
  public_v2:
    community: public
    security_level: noAuthNoPriv
    version: 2
