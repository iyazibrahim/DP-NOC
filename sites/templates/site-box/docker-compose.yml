# Alloy site collector for NUC / site box (one site at a time).
# Configure via deploy.sh (writes .env + devices.json + config.alloy).

services:
  alloy:
    image: grafana/alloy:v1.5.1
    container_name: noc_site_alloy
    restart: unless-stopped
    pid: host
    cap_add:
      - NET_RAW
    network_mode: host
    env_file:
      - .env
    volumes:
      - ./config.alloy:/etc/alloy/config.alloy:ro
      - ./blackbox.yml:/etc/alloy/blackbox.yml:ro
      - ./snmp.yml:/etc/alloy/snmp.yml:ro
      # Host metrics — REQUIRED for CPU/RAM/disk (one mount is enough)
      - /:/rootfs:ro
    working_dir: /etc/alloy
    command:
      - run
      - /etc/alloy/config.alloy
      - --storage.path=/var/lib/alloy/data
      - --server.http.listen-addr=127.0.0.1:12345
